# Future Steps

## Что еще нужно сделать

- Прогнать backend tests после восстановления локального Python/venv.
- Прогнать `npm run build` вне sandbox или в окружении, где Vite может читать `vite.config.ts`.
- Прогнать browser/e2e-проверку полного сценария после успешного старта backend: admin добавляет тренд, пользователь видит `Viral Radar` и `СНГ Viral Gap`, делает Remix, затем видит очки и историю в профиле.
- Перед деплоем сделать backup production database и проверить первый старт backend на staging/сервере, чтобы schema migrations добавили недостающие колонки к старым таблицам.
- Решить продуктово, нужен ли daily cap на очки за Remix, чтобы пользователи не фармили геймификацию повторными генерациями.
- Решить, нужен ли отдельный видимый error state для `СНГ Viral Gap`; сейчас секция скрывается при пустом ответе или ошибке.

## Правило

- Все новые временные решения, моки, заглушки и недоделанные сценарии фиксировать здесь сразу при добавлении.
- Не добавлять emoji в интерфейс и моковые данные; для иконок использовать SVG или `lucide-react`.
